{
  "name": "skylords-live-replay",
  "version": "0.1.0",
  "description": "bla",
  "author": "Kr4pper",
  "main": "index.js",
  "scripts": {
    "build": "rmdir /s /q dist && tsc -b && xcopy replays\\*.pmv dist\\replays\\",
    "start": "node dist/index.js replays\\basic.pmv",
    "restart": "npm run build && cross-env LOG_LEVEL=INFO npm run start",
    "watch": "tsc-watch --onSuccess \"node dist/index.js replays\\convoy.pmv\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC",
  "dependencies": {
    "cross-env": "^7.0.3",
    "ts-node": "^10.9.2"
  },
  "devDependencies": {
    "@types/node": "^22.14.1",
    "tsc-watch": "7.2.0",
    "typescript": "^5.8.3"
  }
}
